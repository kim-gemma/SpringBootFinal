<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boot.data.mapper.MemberMapperInter">
	<insert id="insertMemberform" parameterType="MemberDto">
		insert into member (name, id, pass, photo,email, gaipday) 
		values (#{name},#{id},#{pass},#{photo},#{email},now())
	</insert>
	<select id="getMemberList" resultType="MemberDto">
		select * from member order by member
	</select>
</mapper>